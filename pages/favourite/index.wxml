<yc-empty-view show='{{dataList.length==0}}'></yc-empty-view>

<view class='layout-2'>
  <block wx:for="{{dataList}}" wx:key="{{index}}">
    <image src='{{item}}' lazy-load='true' mode='aspectFit' data-src='{{item}}' bindtap='previewImage'></image>
  </block>
</view>