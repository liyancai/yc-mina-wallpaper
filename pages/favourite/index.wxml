<view class='empty-view' wx:if='{{!dataList}}'>暂无数据!</view>

<view class='layout-2'>
  <block wx:for="{{dataList}}" wx:key="{{index}}">
  <navigator url='../hot/detail?url={{item}}'>
    <image src='{{item}}' lazy-load='true' mode='aspectFit'></image>
  </navigator>
  </block>
</view>